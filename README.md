# git-display-progress project example

[This is a sample project being managed by
`git-display-progress`. See the `main` branch in this repo
for more information.]

`hello.py` prints "hello world". It is not so exciting.
